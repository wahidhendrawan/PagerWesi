name: PagerWesi Audit PR
on:
  pull_request:
    types: [opened, synchronize]

jobs:
  audit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run PagerWesi Audit
        uses: ./
        with:
          target: local
          format: html
